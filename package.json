{
  "name": "crypto-crush-p1",
  "private": true,
  "version": "0.0.0",
  "description": "Crypto Crush P1 — root build authority. Single public demo target: apps/web-public.",
  "buildAuthority": {
    "root": ".",
    "publicDemoTarget": "apps/web-public",
    "outputDir": "dist/web-public",
    "rule": "All public demo builds emit to dist/web-public at the repo root. No app may write build output outside this path. Only apps/web-public is a build target in P1."
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify:build": "node scripts/verify-build.mjs"
  },
  "workspaces": [
    "apps/web-public"
  ]
}
