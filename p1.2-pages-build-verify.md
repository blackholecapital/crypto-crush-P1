# Crypto Crush | P1.2 | Pages Settings + Build Verification

## 1. WB P1.2 — Pages settings update

Applied to `p1.1-shell-demo-route.md` section 4.

Changes from V1.5.1:

- removed subfolder-root deploy instruction (`apps/web-public`)
- Root = repo root
- Build = `npm run build`
- Output = `dist/web-public`
- Branch = `main`

Unchanged:

- launch path mapping (`splash → login/home → level_select`)
- route path table
- SPA fallback
- node version

## 2. WA P1.2 — build verification

### Verification contract

- `npm run build` runs from repo root
- produces `dist/web-public/index.html`
- all required static assets land under `dist/web-public/`
- no build output writes outside `dist/web-public/`

### Verification status

BLOCKED — no buildable scaffold present at repo root.

Observed repo state:

- no `package.json` at repo root
- no `package.json` under `apps/web-public/`
- no existing `dist/` directory
- `apps/web-public/src/` contains layout and mount TypeScript stubs only, no entry HTML, no bundler config

### Required to unblock

- add `package.json` at repo root with `build` script
- add bundler config (e.g. vite/rollup) wired to emit into `dist/web-public`
- add shell entry (`index.html` + mount) under `apps/web-public/src`
- then re-run WA P1.2 verification

## 3. Live test

### Test contract

- redeploy Cloudflare Pages (production branch `main`)
- `/` loads splash
- `splash → login/home → level_select`
- `level_select → game`

### Live test status

BLOCKED — depends on WA P1.2 build verification (section 2) completing.

Cloudflare Pages redeploy cannot succeed until a buildable scaffold at repo root emits `dist/web-public/index.html`.

## 4. Route mapping (unchanged, carried forward)

- `/` → splash
- splash → login (no session) | home (session exists) | home (guest continue)
- home → level select (primary play action)
- level select → game (WA entry payload, approved contract)

## 5. Excluded

- board logic
- score logic
- blocker/booster mechanics
- payment implementation
- chat protocol internals
- leaderboard ranking logic
- auth datastore redesign
- art polish
- contract redesign

## Return at merge gate

`Crypto Crush | BLUEPRINT | V1.5.2`
