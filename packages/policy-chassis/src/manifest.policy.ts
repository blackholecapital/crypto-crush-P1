// Upstream authority: xyz-factory-system/invariants/policies/manifest-policy.md
// DOWNSTREAM STATUS: non-authoritative — mirror only, no reinterpretation

export const MANIFEST_POLICY = {
  authority_root: "xyz-factory-system/invariants/policies/manifest-policy.md",
  downstream_status: "non-authoritative",
  manifest_approval: "canonical only",
  declaration_authority: "canonical only",
} as const;
